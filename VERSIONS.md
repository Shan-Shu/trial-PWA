# Versions

research-desk 的版本登记。**引擎来源**单独标注：本项目 = PWA 界面 × trial 引擎。

| 版本 | 说明 |
|---|---|
| v0.1.0 | **research-desk 起步。** 新建独立项目（旧仓库 `research-agent` 冻结，tag `frozen-before-research-desk`，引擎来源 v0.4.5 @ 36a14fe）。界面：从 PWA（`JoeTrump22/-paper_writing_assistant`）原样搬入 `ui/` 共 1340 行（10 页 + Streamlit 外壳 + `st.fragment` 实时进度 + 深色主题 token），写作台页保留、后续按访谈闭环重做；引擎：`src/research_agent/` 原样搬入（约 21636 行），FastAPI 数据面保留为可选，旧 JS 前端（约 4372 行）刻意未搬入；PWA 后端 5113 行经逐项核对能力已被引擎覆盖，一行未搬。搬入时修掉一个真缺陷：派工的执行函数会落到全局默认库（**对 A 库下单却写进 B 库**），现改为注入本次派工所在库与连接，并让本体重建任务自建 schema。测试 462 → 461（去掉已退役 JS 前端的用例与依赖未搬脚本的 PWA 迁移用例）。 |
| v0.1.1 | **写作台按访谈闭环重做，并修掉三处阻断。** 写作台（`desk/ui/views/writing.py`）不再是"填表生成"，而是访谈闭环：体裁/主题/选部分三问 → 逐部分推进（真实作业 + `st.fragment` 进度 + 可停止）→ 缺口决策 → 正文落库；并支持用自然语言**直接向工作规划节点发需求**，由规划节点解析成方案再分发给其他节点执行（`parse_dispatch_request` → 3 方案 → `create_dispatch`）。适配层补齐访谈/派工方法（+140 行）。三处修复：① `pyproject.toml` 被 UTF-8↔GBK 双重编码污染且换行丢失，把 `packages` 键吞进了注释，构建直接失败；② `sufficiency.evaluate_sufficiency` 直查**懒建**的 `ontology_hyperedges`，空库上第一步判定就抛 `no such table`，把整个访谈作业打断；③ `model_missing_behavior` 在登记表里只被声明、**全引擎无人读取**，于是离线跑派工仍真调模型并打出 arXiv / Semantic Scholar / OpenAlex 请求（实测撞 600s 超时）——现由执行器按 `needs_model` + `unavailable` 如实跳过，适配层把离线信号传入执行器。新增回归 `scripts/smoke_desk_dispatch.py`（整库复制后派工，正式库只读）。验证：引擎单测 411 OK、10 页渲染 10/10、访谈闭环 11/11、派工闭环 14/14。 |
| v0.1.2 | **正文质量：让成段真正拿到"具体的东西"。** 修掉三处让正文空泛的引擎缺陷。① `build_materials` 只把条件与测量的**条数**交给模型（"30 项条件"），模型无从下笔——现改为取明细并渲染成 `temperature = 100 °C`、`equivalent = 4 equiv`、`yield 92 %` 这样的具体值（`format_condition` / `format_measurement`）。② `compose_node` **从不读 `state["knowledge"]`**，知识消费节点归纳的机制状态与候选算子链在成段这一步被整体丢弃——现整包接入提示词（新增 `render_knowledge_digest`，带 `H-xxxx` 溯源；无溯源的条目显式标「属推断」），并把消费节点的顶层状态（`consumed` / `needs_collection` / `consumer_failed`）一并捎上。③ `consumer_model` 在接口上一直存在，但**所有写作路径都不传**（只有 `dashboard/app.py` 的研究链路传），消费节点因此永远走 `offline_fallback`、`confidence=0.0` —— 现由写作台写作步骤与数据面 `compose_section` 两处入口构建（新增 `default_role_model(role)`，缺 Key 时优雅退回确定性兜底）。另外发现并修掉一个会**整段交白卷**的坑：命中文献为 0 时 `build_materials` 直接返回 `[]`，把库里已有的超边与条件一并丢掉——现退回**由证据编号回溯归属文献**（每条超边自带 `paper_key`）。实测真实库（27 个写作项目、233 条超边）单节提示词由 4610 字符增至 16157 字符，含真实数值与 12 条机制状态 / 8 条算子链。新增只读诊断 `scripts/show_compose_prompt.py`（整库复制、不调模型、不花配额）。另外修掉一个**测试污染正式库**的缺陷：`test_issue_batch2` / `test_study_nodes` / `test_retrieval_skills_control` 里 8 处构造节点时没传 `conn`/`settings`，节点回落到默认库 `data/research_agent.db` 并写入 `processing_log`（实测一轮 418 项写 20 行）；已逐处改为传临时库，并新增 `scripts/run_tests.py`（把 `RA_DB_PATH` 指向临时文件 + 跑完对账真实库快照，一旦被写入即报错）。验证：引擎单测 411 → 418 OK、10 页渲染 10/10、访谈闭环 11/11、派工闭环 14/14。 |
| v0.1.3 | **写作台加「项目管理」：看得清、改得了、删得干净。** 缘起：真实库 27 个写作项目里只有 1 个写过正文，其余是历史冒烟残留（`aaa`、`浏览器冒烟`、`probe-*`），而界面只能选项目、不能管。① **概况**：项目列表补 `filled_sections`/`total_sections`/`dispatch_count`/`created_at`，管理区表格摆出标题/体裁/已写节数/更新时间，让"不用的项目"一眼可辨（只给标题分不出哪条能删）。② **重命名**：`service.rename_project`（标题与主题分开给，标题不许改空），输入框 key 带 pid 以带出各项目现值。③ **删除**：`service.delete_project` 改为**真正删干净并回报行数**——`writing_sections`/`section_runs` 有 `ON DELETE CASCADE` 且 `connect()` 开了 `PRAGMA foreign_keys=ON`，但 **`dispatch_runs.project_id` 没有外键**，此前不显式清理会留下孤儿派工单（`list_dispatches` 正是按 project_id 过滤，会捞到已删项目的单子）；新增 `batch_delete_projects`（不存在的 id 不报错、去重）与适配层 `delete_writing_project`/`batch_delete_writing_projects`/`rename_writing_project`。④ **写作台改为「项目列表页 → 项目详情页」两页结构**（用户反馈驱动）：此前只有一页，"选中项目"是在折叠区里用下拉**隐式**完成的——既看不到进入某个项目的入口，也没有任何抬头说明你在哪个项目里；更糟的是选中后自动推进一起作业就把页面主体顶掉，实测真实库上渲染出来只有「两个折叠区 + 0% 排队中」，对话/部分/指令区一片空白。现在列表页每行一个「打开」按钮，详情页有 `#### #id · 标题` 抬头（含体裁/主题/已写节数/更新时间）与「← 全部项目」返回；推进作业时**不再** `return` 掉内容，进度与内容并存。删除当前打开的项目会清掉选中态并回列表。界面按用户选择只做**二次确认勾选**（不做强制导出），并额外守住"正在跑作业的项目不许删"（该判断抽成纯函数 `_blocked_by_running_job` 并单测 4 项——它的失败后果落在引擎表上：作业落库时往 `writing_sections` 写指向已删项目的行会撞外键）。新增界面级回归 `scripts/smoke_desk_projects.py`（24 项，真点导航、改名与删除，全程只用临时库）。验证：引擎单测 418 → 428 OK、10 页渲染 10/10、访谈闭环 11/11、派工闭环 14/14、项目管理 24/24。 |
| v0.1.4 | **修掉一个会让「补检」白跑的库路由缺陷；并更正上一轮的一处误判。** ① **缺陷**：适配器只给 `db_path`、不给 `settings` 时，`self.db_path` 指向目标库，但**转发给引擎的 `settings` 仍是全局单例**（指向 `data/research_agent.db`）。适配器自己的查询走 `self.db_path`，所以界面看起来一切正常；而凡是按 `settings.db_path` 取连接的引擎路径就把结果写进了**另一个库**——最典型的是补检里的 `run_topic`（`db = conn or connect(s.db_path)`）。实测后果：启动器明明用 `desk.db`，一次跑偏的补检却把 `research_agent.db` 从 1.1 MB 撑到 130 MB（21 篇文献 + PDF，`processing_log` 579 行），而 `desk.db` **一篇都没加**——用户点完「补检」等于白跑。这是"对 A 库操作却写进 B 库"的同类缺陷。修法：给了 `db_path` 而没给 `settings` 时，用 `dataclasses.replace` 复制一份并改写 `db_path`（**不就地改全局单例**，那会污染同进程其它调用方）。新增 4 项回归，其中一项直接断言"由适配器 settings 构建出的检索服务栈"与适配器同库——把修复接到真正写库的那条路径上，而不是只测构造函数。② **更正上一轮判断**：v0.1.2 记的"测试污染默认库"里，3 个文件共 20 行是**真问题**（已修）；但后来观察到的 +8/+2 **并非测试所致**，而是**上一次会话遗留的 Streamlit 进程仍在跑补检**、与测试并发写入同一库，被护栏误判——"随机归因到不同用例 + 随机论文 key"就是并发写入的特征，我当时没识别出来，还顺着假线索去找测试。残留进程已清理（含占用 8501 的那个，以及与本项目无关的旧仓库 dashboard/API 两个）。结论：裸跑 `python -m unittest discover -s tests` 现已确认干净（默认库行数不再变化），因此**不需要**为测试加环境重定向的额外网（已删除为此加的 `tests/__init__.py`，它在 `discover -s tests` 下根本不会执行）。③ **启动器加固**：`停止助手.bat` 原先只按端口杀"正在监听"的那个进程，两种情况会漏——实例卡死/健康探测超时（根本进不了 FOUND），以及启动器是"父进程 `serve_desk.py` → 子进程（真正监听）"两层、只杀子进程会留下父进程。漏掉的实例仍在后台跑检索与模型调用（本次就是它把补检写进了错误的库）。新增 `scripts/kill_strays.py`：按**命令行特征**（覆盖 `scripts\serve_desk.py`、`src\desk\ui\app.py`、`desk\app.py` 三种启动方式）兜底清杀，与端口无关，卡死实例同样杀得掉；`.bat` 在端口扫描之后直接调用它并让它自己报告（**没有**用 `for /f` 取计数——cmd 的 for /f 在命令以引号开头时会吃掉引号，实测直接报"文件名语法不正确"，而这里的路径必须带引号才稳）。实测三种场景：扫描范围外的 8599 实例被兜住并清掉、正常 8501 起停干净、空跑时如实报告。验证：引擎单测 428 → 432 OK（护栏对账通过）、10 页渲染 10/10、访谈闭环 11/11、派工闭环 14/14、项目管理 24/24。 |

## 来源对照

| 部分 | 来源 | 备注 |
|---|---|---|
| `src/research_agent/` | trial（原 `Shan-Shu/trial`，后为 `research-agent`）v0.4.5 @ `36a14fe` | 引擎本体，逻辑未改 |
| `src/desk/ui/` | PWA `JoeTrump22/-paper_writing_assistant` | 界面本体，仅改数据来源引用 |
| `packs/` | trial | 领域内容（零硬编码的来源） |
| `tests/` | trial | 引擎安全网 |

## 刻意未搬入

| 部分 | 行数 | 原因 |
|---|---:|---|
| `dashboard/static/`（旧 JS 前端） | 约 4372 | 界面改用 Streamlit，此套退役 |
| PWA 后端（`paper_assistant/` 除 `ui/`） | 5113 | 能力已被引擎覆盖（含本体可视化：引擎 `render_interactive_html` 有本地 vendor 挂载点 + 静态 SVG + 文件导出） |
| `tests/test_migration.py`、`tests/test_frontend_assets.py` | — | 分别依赖未搬入的 PWA 迁移脚本、已退役的 JS 前端 |
| 多跳图搜索（PWA `retrieval/graph_search.py`） | 约 200 | 复杂度过高（邻接矩阵两两展开，O(n³)），在本体规模（7000+ 节点）上实际不可用。**明确决定不要**，不是待办 |
